https://tiktokenizer.vercel.app/    查看分词网站

### 分析式AI
也称为判别式Al，其核心任务是对已有数据进行分0类、预测或决策。
优势在于其高精度和高效性，但其局限性在于仅能处理已有数据的模式，无法创造新内容。
### 生成式AI
专注于创造新内容，例如文本、图像、音频等。
突破在于其创造性和灵活性，但也面临数据隐私、版权保护等挑战


大语言模型是一种通用自然语言生成模型，使用大量语料数据训练，以实现生成文本、回答问题、对话生成等
![[Pasted image 20260615225444.png]]
![[Pasted image 20260615225506.png]]
## 模型的常见特殊Token
为了让模型更好地理解文本的结构和指令，开发者会预设一些具有特殊功能的Token。
这些Token不代表具体词义，而是作为一种“标点”或“命令"存在。
#### 分隔符(Separator Token):
用于区分不同的文本段落或角色。比如，在对话中区分用户和AI的发言，可能会用<|userl>和<|assistant|>这样的Token。
#### 结束符(End-of-Sentence/End-of-Text Token):
告知模型文本已经结束，可以停止生成了。常见的如[EOS]或<lendoftext|>。这对于确保模型生成完整且不冗长的回答至关重要。
#### 起始符(Start Token):
标记序列的开始，例如[CLS](Classification)或[BOs](Beginning of Sentence)，帮助模型准备开始处理文本。

[ 魔搭社区](https://www.modelscope.cn/home)

##### 大模型最关键的两个文件：

Tokenizer.json: 作为分词的字典，表明中文该怎么切分，转化成数值之后，在模型中完成各种各样的计算，计算之后得到token的ID，反变换，得到中文的结果

Model:大模型的神经网络的参数，权重文件71GB


#### 大模型中的Temperature, Top P的原理和作用是什么?

=>控制LLM生成文本的多样性，但原理不同。
Temperature(温度):
原理:在模型计算出下一个Token所有可能的]概率分布后，Temperature会调整这个分布的“平滑度”。
高Temperature(如1.0+):会让低概率的Token更容易被选中，使生成结果==更具创造性==，可能出现不连贯的词语。
低Temperature(如0.2):会让高概率的Token权重更大，使生成==结果更稳定、更符合训练数据，但会更保守。==

Top P(核采样):
###### 原理:它设定一个概率阈值(P)，然后从高到低累加所有Token的概率，直到总和超过P为止。模型只会在这个累加出来的“核心”词汇表中选择下一个Token。
高Top P(如0.9):候选词汇表较大，结果更多样。
低Top P(如0.1):候选词汇表非常小，结果更具确定性。
假设模型要完成句子:“今天天气真..."
模型预测的下一个词可能是:好(60%)、不错(30%)、糟(9%)、可乐(0.01%)。
高Temperature:会提升所有词的概率，使得“可乐”这个不相关的词也有机会被选中。
Top P(设为0.9):会选择概率总和达到90%的词。这里好(60%)+不错(30%)=90%，所以模型只会从“好"和“不错”中选择，直接排除了“可乐”这种离谱的选项。

相比Temperature，Top P能更动态地调整候选词的数量，避免选到概率极低的离谱词汇=>产生更高质量的文本。

Top P > Temperature
[大模型服务平台百炼 - 大模型应用构建 - 阿里云](https://www.aliyun.com/product/bailian)


### AI大模型聊天产品的超能力
##### 超能力1:联网搜索
弥补LLM训练数据截止日期的限制=>获取外部信息
当用户提问涉及最新资讯时，系统会识别出这一需求，自动调用搜索Tool，并将问题转化为多个简洁的搜索关键词。
接着，程序调用搜索引擎API(如Google搜索)获取信息。
最后，这些实时信息会作为上下文提供给模型，由模型进行总结和提炼，生成精准且与时俱进的回答。
例如，当你询问“黄金的涨跌和哪些因素有关?”
LLM会调用一个搜索工具，输入你刚才的问题，然后获取相关的信息
=>整理到回答中。

大模型如果没有网络，就是一堆参数
input => model (知识量45TB)=> output    使用的是模型的自己的知识和推理结果

AI产品=LLM+Function Call（Web Fetch）+RAG

function call 是在代码里写好的函数，给LLM进行定制
MCP：是第三方服务，模型上下文协议           都是调用工具，一个就是把源代码给你，在你内部自己去跑，一个是外部协议，看不到里面具体实现，根据http这样一种协议的方法去请求

Q:联网获取到的内容怎么提炼的呢
例：黄金价格2026年4月23日=>Web Search=>作为知识库进行返回作为上下文，返回的是原文LLM会根据这个返回的知识+用户query=>结果

Q：他是怎么知道自己什么时候需要调用web fetch
prompt 写上你都有哪些工具可以使用=>LLM
用户query+Tool定义(告诉大模型，如果你想要用这个Tool的话，可以返回这个工具的名称以及调用的参数)
=>LLM返回，(好的，我打算调用web search工具，传入参数=)
=>截流出来LLM提取的工具名称和参数，在本地执行这个函数（这就是为什么需要把源代码放到本地的原因，因为它要掉函数在本地通过他的代码去运行它，运行完结果就出来了）
=>LLM就看到了工具运行后的结果=>告诉用户

本质上是RAG：
Prompt Engineering=>Context Engineering

Q:大模型中的知识是怎么存储的?
知识存储在 model的权重中, https://modelscope.cn/models/Qwen/Qwen3.6-35B-A3B/files
input => model => output
香蕉是什么=>model=>香蕉是一个banna .....
model通过多次迭代(10万轮，更新权重，找到一个输出结果更好的，Loss误差更小)

不完全叫存储（45TB数据=>压缩到了1TB的参数中）
压缩即智能

##### 超能力2:读取文件
Al读取文件并非简单打开文档，而是将不同格式翻译成模型能理解的结构化数据:
PDF/扫描件:先通过oCR提取文字，再送生行版式分析一一识别标题层级、段落边界、表格结构，甚至处理双栏混本或Markdown格式。排和图文环绕，最终转化为带标记的文
·Word/Excel:直接解析底层XML结构，保留文档大纲、表格数据、公式和样式信息，遏免纯文本化导致格式混乱。器(ViT)提取图像特征，与语言模型对齐，实·图片:调用多模态能力，通过视觉编码现"看图说话"、图表理解甚至手写识别。
·音频/视频:先经ASR转为文本，或提取关键帧按图像处理。

LLM+read_file

#### 超能力3:记忆功能(从金鱼到伙伴)

LM本身是无状态的，每次对话都是一次全新的互动，不记得之前的交流。为了实现“记忆”，系统会在每次对话时，将最近的几轮问答作为背景信息一起发送给模型=>称为“短期记忆”或“上下文窗口”。
对于需要长期记住的关键信息，例如你的名字或偏好，系统会通过特定算法提取这些信息，=>将其存储在用户专属的数据库中。
=>在后续的对话中，系统会先从数据库中读取，为模型提供更个性化的背景知识。
比如，你告诉Al"我喜欢简洁的回答风格”，系统会记录这一偏好。
下次你提问时，它就会倾向于给出更简练的答复。

http://localhost:8889/
.py 是Python源脚本, 通过IDE (Cursor, Trae, Lingma,VSCode)直接运行


LLM能聊天
LLM能调工具
多模态LLM能识别图像的内容
LLM 能调工具(解决运行场景，配置系统监测的工具）

